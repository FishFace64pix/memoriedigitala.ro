📧 RACKHOST SMTP AYARLARI

═══════════════════════════════════════════════════════════

✅ SMTP BİLGİLERİNİZ:
─────────────────────────────────────────────────────────────

Server: cp01.rackhost.eu
Port: 465 (Securizat SSL)
Port: 587 (TLS - Alternatif)
Port: 25 (Non-secure - önerilmez)

═══════════════════════════════════════════════════════════

📧 1. EMAIL ADRESİ OLUŞTURUN:
─────────────────────────────────────────────────────────────

cPanel'de:
1. Email Accounts → Create
2. Email: noreply@yourdomain.ro
3. Password: Güçlü şifre (örn: Memorie2024!)
4. Create

═══════════════════════════════════════════════════════════

🔧 2. .ENV.LOCAL AYARLARI:
─────────────────────────────────────────────────────────────

Mevcut dosyayı açın ve ekleyin:

# Rackhost Email Settings
SMTP_HOST=cp01.rackhost.eu
SMTP_PORT=465
SMTP_USER=noreply@yourdomain.ro
SMTP_PASS=your-email-password
FROM_EMAIL=noreply@yourdomain.ro

ÖRNEK:
SMTP_HOST=cp01.rackhost.eu
SMTP_PORT=465
SMTP_USER=noreply@memoriedigitala.ro
SMTP_PASS=GüçlüŞifre123!
FROM_EMAIL=noreply@memoriedigitala.ro

═══════════════════════════════════════════════════════════

⚙️ 3. SECURED CONNECTION:
─────────────────────────────────────────────────────────────

Port 465 için lib/email.ts'de secure: true olmalı:

const transporter = nodemailer.createTransport({
  host: process.env.SMTP_HOST,
  port: parseInt(process.env.SMTP_PORT || '465'),
  secure: true,  // 465 için true
  auth: {
    user: process.env.SMTP_USER,
    pass: process.env.SMTP_PASS,
  },
});

═══════════════════════════════════════════════════════════

🧪 4. TEST ET:
─────────────────────────────────────────────────────────────

1. .env.local'e ayarları ekleyin
2. Uygulamayı restart edin
3. Yeni order oluşturun
4. Email'in gelip gelmediğini kontrol edin

═══════════════════════════════════════════════════════════

📝 NOTLAR:
─────────────────────────────────────────────────────────────

✓ Port 465 = SSL (secure: true)
✓ Port 587 = TLS (secure: false, güvenlik var)
✓ Port 25 = Non-secure (kullanmayın)

CPanel'de email oluştururken:
- Güçlü şifre kullanın
- noreply@ kullanmanızı öneriyoruz
- Email limit: 2GB

═══════════════════════════════════════════════════════════


