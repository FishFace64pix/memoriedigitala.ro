📧 EMAIL ÇÖZÜMÜ

═══════════════════════════════════════════════════════════

ÇÖZÜM 1: Email'i geçici olarak disable edin
─────────────────────────────────────────────────────────────

app/api/create-order/route.ts dosyasında email gönderme
kısmını yorum satırı yapın:

// Send confirmation email
const adminUrl = `${appUrl}/admin/${eventId}`;
const eventUrl = `${appUrl}/e/${accessKey}`;

/*
try {
  await sendOrderConfirmationEmail(...);
} catch (emailError) {
  console.error('Failed to send email:', emailError);
}
*/

═══════════════════════════════════════════════════════════

ÇÖZÜM 2: Gmail kullanın (daha kolay)
─────────────────────────────────────────────────────────────

.env.local:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
FROM_EMAIL=your-email@gmail.com

═══════════════════════════════════════════════════════════

ÇÖZÜM 3: Production'da kullanın
─────────────────────────────────────────────────────────────

Şimdilik email olmadan devam edin.
Production'a deploy edince SMTP çalışır.

═══════════════════════════════════════════════════════════


